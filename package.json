{
  "dependencies": {
    "@mui/icons-material": "^5.8.3"
  },
  "scripts": {
    "start": "node api/bin/www",
    "heroku-postbuild": "npm install && cd api && npm install && cd ../frontend && npm install && npm run build"
   }
   
}
